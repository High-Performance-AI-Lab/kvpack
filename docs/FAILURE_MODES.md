# Failure-mode matrix

The matrix lives in the runbook: `docs/SPARK_GX10_E2E_RUNBOOK.md`, section
**B13. Failure-mode matrix and hardening (2026-08-01)**. Update it there;
this file remains only as a stable pointer for the F-row references in the
injection tests (`scripts/gx10/test_transport_injection.py`,
`scripts/gx10/llamacpp/test_failure_injection.py`,
`scripts/test_driver_failfast.py`).
